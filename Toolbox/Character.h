#if !defined(_Toolbox_Character_h)
#define _Toolbox_Character_h

  #include <string>

namespace Toolbox
{
    namespace Character
    {
        bool isOctalDigit(char);
        char octalToBinary(char);
        char hexToBinary(char);
    }
}

  #endif
